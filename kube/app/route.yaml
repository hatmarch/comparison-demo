apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: spring-petclinic
  name: jws-app
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: jws-app
    weight: 100
